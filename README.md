# weatherApi

Weather API Integration built with Java 17 and Spring Boot. Fetches real-time weather data (temperature, humidity, forecast) from a third-party API and exposes it via RESTful endpoints. Tech: Java 17, Spring Boot, REST API, JSON parsing.
